const Banner = ({image}) => {
    return (
        <div class="mb-10">
            <img src={image} alt="Afriluck Lotto" class="w-full h-48 object-contain" />
        </div>
    );
};

export default Banner;